# Prompts Starter Directory

This directory is reserved for future prompt assets used by the `research`
flow.

At the current stage, it is intentionally present as a starter surface rather
than a claim that a runtime prompt system is already public.
