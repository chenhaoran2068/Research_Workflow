# Templates Starter Directory

This directory is reserved for future templates used by the `research` flow.

Examples could later include:

- task cards
- report shells
- review forms
- claim calibration templates

Its current presence means starter structure exists, not that every template is
already public.
