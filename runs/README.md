# Runtime Outputs

This directory is reserved for local runtime outputs.

Current rule:

- generated dry-run outputs are written under `runs/generated/`
- generated outputs are intentionally ignored by git

This repository does **not** claim that committed run outputs here equal the
full internal runtime archive.
