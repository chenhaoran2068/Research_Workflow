# Host Profiles

This directory contains public sample host profiles for the minimal runtime
skeleton.

Current samples:

- `codex_cli.json`
- `claude_code.json`

Each run should bind exactly one host profile.

These files do **not** contain secrets or provider credentials.
